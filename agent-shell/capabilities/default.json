{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability utama Companion (origin lokal, halaman ter-embed): jendela main + pet boleh memakai API jendela native (drag, klik-tembus, destroy). Perintah aplikasi (invoke_handler) diizinkan untuk origin lokal tanpa capability tambahan.",
  "windows": ["main", "pet"],
  "permissions": [
    "core:default",
    "core:window:allow-set-ignore-cursor-events",
    "core:window:allow-is-visible",
    "core:window:allow-set-focus",
    "core:window:allow-destroy",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show"
  ]
}
