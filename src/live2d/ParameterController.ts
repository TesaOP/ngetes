/** Fase 4 — skeleton. Nanti: get/set parameter model-agnostic (Fase 8). */
export class ParameterController {
  getParameter(_id: string): number {
    throw new Error("ParameterController belum diimplementasi — Fase 8");
  }
  setParameter(_id: string, _value: number): void {
    throw new Error("ParameterController belum diimplementasi — Fase 8");
  }
}
