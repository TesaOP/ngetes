/** Fase 4 — skeleton. Nanti: wrapper CubismFramework + CubismUserModel + renderer. */
export class CubismRuntime {
  constructor(_gl: WebGL2RenderingContext | WebGLRenderingContext) {
    throw new Error("CubismRuntime belum diimplementasi — Fase 5-6");
  }
}
